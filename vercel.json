{
  "version": 2,
  "name": "blog-server",
  "builds": [
    {
        "src": "dist/(.*).js",
        "use": "@vercel/node"
    }
  ],
  "routes": [
    {
        "src": "/(.*)",
        "dest": "dist/(.*).js"
    }
  ]
}